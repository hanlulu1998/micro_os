# micro_os
Reproducing "Writing an OS in Rust (First Edition)" with current Rust
## 安装必备组件
1. 安装rust源码
```bash
rustup component add rust-src
```
2. 安装编译组件
```bash
sudo apt install nasm xorriso grub2 qemu-system
```